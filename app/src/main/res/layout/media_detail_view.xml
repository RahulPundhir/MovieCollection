<?xml version="1.0" encoding="utf-8"?>
<com.android.movies.media.MediaDetailsView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:background="@color/colorPrimary"
        android:titleTextColor="@android:color/white" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/toolbar"
            android:background="#000000"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/video_image"
                android:layout_width="match_parent"
                android:layout_height="250dp"
                android:scaleType="fitXY"
                android:src="@drawable/samplemovie" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/standard_margin"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/video_title"
                    style="@style/TextViewStyle" />

                <TextView
                    android:id="@+id/video_rated"
                    style="@style/TextViewStyle"
                    android:layout_marginTop="@dimen/margin_8dp" />

                <TextView
                    android:id="@+id/video_released_date"
                    style="@style/TextViewStyle"
                    android:layout_marginTop="@dimen/margin_8dp" />

                <TextView
                    android:id="@+id/video_director"
                    style="@style/TextViewStyle"
                    android:layout_marginTop="@dimen/margin_8dp" />

                <TextView
                    android:id="@+id/video_writer"
                    style="@style/TextViewStyle"
                    android:layout_marginTop="@dimen/margin_8dp" />

                <TextView
                    android:id="@+id/video_actors"
                    style="@style/TextViewStyle"
                    android:layout_marginTop="@dimen/margin_8dp" />

                <TextView
                    android:id="@+id/video_plot"
                    style="@style/TextViewStyle"
                    android:layout_marginTop="@dimen/margin_8dp" />

            </LinearLayout>
        </LinearLayout>

    </ScrollView>
</com.android.movies.media.MediaDetailsView>